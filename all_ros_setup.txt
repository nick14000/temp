
(if not in sudoers) ---

(use nano instead of vi, works better)

su -

<enter password followed by enter>

vi /etc/sudoers

<go to "# User privilege specification">

<add the following line under root>

<your username> ALL=(ALL:ALL) ALL

:q 

logout

(end if not in sudoers) ----


First off, the essentials
sudo apt update
sudo apt install neofetch (definetly 100000000million%neccessary)
sudo apt install vim
sudo apt install neovim
sudo apt install build-essential


sudo apt-get update

sudo apt-get install htop

sudo apt install -y gdb-source

sudo apt install cmake

sudo apt update

sudo apt install python3-serial


(at this point go to sotware, check all the boxes, make sure you are on 'main' server not Canada.)


sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu $(lsb_release -sc) main" > /etc/apt/sources.list.d/ros-latest.list'

sudo apt install curl


(if not in sudoers) ---

(use nano instead of vi, works better)

su -

<enter password followed by enter>

vi /etc/sudoers

<go to "# User privilege specification">

<add the following line under root>

<your username> ALL=(ALL:ALL) ALL

:q 

logout

(end if not in sudoers) ----


First off, the essentials
sudo apt update
sudo apt install neofetch (definetly 100000000million%neccessary)
sudo apt install vim
sudo apt install neovim
sudo apt install build-essential


sudo apt-get update

sudo apt-get install htop

sudo apt install -y gdb-source

sudo apt install cmake

sudo apt update

sudo apt install python3-serial


(at this point go to sotware, check all the boxes, make sure you are on 'main' server not Canada.)


sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu $(lsb_release -sc) main" > /etc/apt/sources.list.d/ros-latest.list'

sudo apt install curl

curl -s https://raw.githubusercontent.com/ros/rosdistro/master/ros.asc | sudo apt-key add –

sudo apt-get update

sudo apt install ros-noetic-desktop-full

sudo rosdep init

rosdep update

echo "source /opt/ros/noetic/setup.bash" >> ~/.bashrc

source ~/.bashrc

sudo apt install python3-rosdep python3-rosinstall python3-rosinstall-generator python3-wstool build-essential

rosversion -d

sudo apt-get update

sudo apt install ros-noetic-desktop-full

sudo rosdep init

rosdep update

echo "source /opt/ros/noetic/setup.bash" >> ~/.bashrc

source ~/.bashrc

sudo apt install python3-rosdep python3-rosinstall python3-rosinstall-generator python3-wstool build-essential

rosversion -d